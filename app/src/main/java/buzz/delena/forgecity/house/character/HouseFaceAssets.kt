package buzz.delena.forgecity.house.character

/** Shared face-card PNG under `assets/` (Adreno-safe ≤512). */
object HouseFaceAssets {
    /** User-provided face applied to all house humanoids (0.15). */
    const val SHARED_FACE = "faces/siva.png"
}
